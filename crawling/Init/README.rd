Dossier qui rassemble tous les fichiers utiles à l'initialisation:
   imports.py : vérifie si tous les modules nécessaires au programme sont installés sur le poste client
   init_config.py : rassemble toutes les constantes chargées lors de l'initialisation et qui seront partagées par plusieurs modules
